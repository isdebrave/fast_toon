import 'package:flutter/material.dart';

class BestChallengeScreen extends StatelessWidget {
  const BestChallengeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
